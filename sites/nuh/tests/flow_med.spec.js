import { test, expect } from '../fixtures/auth.js';

test('NUH Test Example', async ({ authenticatedPage }) => {
  const page = authenticatedPage;
  // Already logged in to NUH site
  // Add your NUH-specific tests here
});
