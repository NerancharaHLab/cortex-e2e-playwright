import { test, expect } from '../fixtures/auth.js';

test.describe("MED", () => {
    test('Step', async ({ authenticatedPage }) => {
        const page = authenticatedPage;
        // Test logic here
    });

    test('Existing Patient Create New Visit', async ({ authenticatedPage }) => {
        const page = authenticatedPage;
        // Test logic here
    });
});